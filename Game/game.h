// Use "#pragma once" to make sure the header is only included once
#pragma once

// There should be a function PROTOTYPE named "Start"
// The function has no return value (the return type is void)
// The function has no parameters
void Start();

// There should be a Boolean variable PROTOTYPE named "playing"
extern bool playing;

// There should be a function PROTOTYPE named "Update"
// The function has no return value (the return type is void)
// The function has no parameters
void Update();

// There should be a function PROTOTYPE named "End"
// The function has no return value (the return type is void)
// The function has no parameters
void End();